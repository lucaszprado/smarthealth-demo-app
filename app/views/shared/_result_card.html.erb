<!-- Reading Partial variables-->
<% link = locals[:link] %>
<% icon_path = locals[:icon_path] %>
<% title = locals[:title] %>
<% key_info = locals[:key_info] %>
<% status = locals[:status] %>
<% date = locals[:date] %>
<% labels = locals[:labels] %>
<% data_type = locals[:data_type] %>

<!-- Rendering Partial -->
<%= link_to link, class: "text-decoration-none link-dark" do %>
  <div class="ResultCard border-top ps-1 u-flex u-flex--justifyBetween u-flex--alignCenter"> <!--ps-3-->
    <!-- Icon component -->
    <div class="me-1">
      <i class=" <%= icon_path %> "></i>
    </div>

    <!-- Title + Label Component -->
    <div class="ResultCard-content">
      <!-- Title Component -->
      <p class="ResultCard-title u-fontSizeVW--primary u-textMargin--noMargin"> <%= title %> </p>

      <!-- Label Component -->
      <% if labels.present? %>
        <div class="LabelsContainer">
          <% labels.each do |label| %>
            <p class="Label Label--<%= label_class(label) %> u-fontSizeVW--quaternary"> <%= label %></p>
          <% end %>
        </div>
      <% end %>
    </div>

    <!-- Key Infos = Source Specific info + Status + Date Component -->
    <div class="ResultCard-keyInfos u-textAlign--right mb-1">

      <div class="u-flex u-flex--justifyBetween u-flex--alignCenter">
        <!-- Source specific component -->
        <p class="u-fontSizeVW--<%=card_text_size(data_type)%> mb-1"><%= key_info %></p>

        <!-- Status component -->
        <% if status.present? %>
          <span class="ResultCard-statusIndicator is-status--<%= status %> mb-1"></span>
        <%end%>

      </div>

      <!-- Date component -->
      <div>
        <p class="u-fontSizeVW--tertiary u-textMargin--noMargin"> Último exame: <%= date.strftime("%d/%m/%Y") %></p>
      </div>
    </div>

  </div>
<% end %>
